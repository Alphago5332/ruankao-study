# 08_答题系统 · 填空专练

> 从知识点清单抽取高频必考点 → 填空题型 → 即时判分 → 错题自动入库 → 答对自动进入下一题

## 目录

- [`index.html`](index.html) — 答题系统主页（直接浏览器打开）
- [`题库.json`](题库.json) — 填空题题库（与 HTML 内嵌同步，便于版本管理）

## 核心特性

| 特性 | 说明 |
|---|---|
| **填空题型** | 题干中 `____` 处为可填 input，单空/多空/并列空都支持 |
| **即时判分** | 填完按 Enter 或点「提交」；宽松匹配（忽略空格、标点、大小写） |
| **答对自动下一题** | 答对延迟 1.2s 自动跳下一题；答错显示「跳过 / 下一题」 |
| **错题本** | 答错的自动收录，底部可点直接重做；支持导出为 .md |
| **进度联动** | 自动检测 `1.x_知识点清单_可勾选.html` 的勾选状态；答对题目后将自动同步勾选 |
| **章节隔离** | 章节 tab 切换；进度统计按章节展示 |
| **持久化** | 全部状态存 localStorage，刷新/重启浏览器不丢失 |

## 出题策略

按优先级：
1. **未答对 + 答错过**（最高优先级，先扫盲）
2. **未答过**（新题）
3. **全部答对**（随机复习）

## 当前题库

| 章节 | 题数 | 高频点 |
|---|---|---|
| 1.1 信息与信息化 | 10 | 信息定义、质量属性、三结合、生命周期、信息化六要素、数字经济 |
| 1.2 现代化基础设施 | 12 | 新基建归属、工业互联网四定位 / 四层级、六大应用模式、车联网端管云 |
| 1.3 现代化创新发展 | 12 | 农业现代化核心手段、乡村振兴三重点、两化融合四方面、智能制造五自 / GB/T 39116 五级 / 能力要素、消费互联网两属性 |
| 1.4 数字中国 | 12 | 主旋律三数字、技术经济范式、统计分类 5 类归属、数据价值化三化、数字政府着力点 / 五特征 / 跨省通办、数字民生三重点、智慧城市五要素 / 五级、数据要素市场「结果非手段」、三法一条例 |
| 1.5 数字化转型与元宇宙 | 12 | 数字化转型定义（新建业务模式）、概念递进三级、三元空间、四科学范式（经验 / 数据密集型）、社交网络信息四性、智慧主体 20 年、能力因子五层级、互联网+ 脱离自然人、S8D / DIKW、数据—智慧过程四步、元宇宙四特征、演进链条 |

**总计 58 题**，覆盖第 1 章已学节次（1.1 ~ 1.5）全部高频必考点。

> 🔧 **题库同步**：`题库.json` 由 `tools/sync_bank.js` 从 `index.html` 内嵌 `QUESTION_BANK` **反向生成**（HTML 为唯一真源）。改题只需改 `index.html`，然后执行
> `node tools/sync_bank.js` 即可保证两份逐字段一致，不再手工双写。

## 与可勾选 HTML 的联动机制

启动后扫描 localStorage 中所有形如 `01_教材笔记/.../知识点清单.md:L行号` 的 key（这是 `1.x_知识点清单_可勾选.html` 写入的命名空间）。

| 检测到 | 顶部面板 |
|---|---|
| 有勾选数据 | 🔗 已检测到 N 个清单，M 项已掌握。答对自动同步。 |
| 无勾选数据 | ⚠ 未检测到。请先在 1.x 可勾选版打卡。 |

**v1 状态**：仅读取并展示，后续会扩展为「答对题目 → 自动同步勾选到对应章节」。

## 使用流程

1. **打开 `index.html`**（直接双击 / WorkBuddy 预览均可）
2. **选择章节**（1.1 / 1.2 / 全部）
3. **填空 → 提交**：判分即时反馈
4. **答对**：自动进入下一题
5. **答错**：显示正确答案 + 提示 + 出处；自动入错题本
6. **底部错题本**：点击任意错题可立即重做

## 后续扩展方向

- [ ] 答题记录 → 每日打卡联动（写入 `00_备考计划` 打卡表）
- [ ] 答对 → 自动同步勾选到对应 1.x 可勾选 HTML
- [x] 1.5 章节题库补全（12 题，2026-09-14 完成，共 58 题）
- [ ] 1.6 本章练习题库（整章串联题，第 1 章收尾时做）
- [ ] 选择题 / 简答题 / 计算题等题型扩展
- [ ] 错题二次练习模式（艾宾浩斯曲线：+1/+3/+7/+14 天）

## 文件命名约定

后续 1.5 及第 2 章起章节题库命名：
```
题库.json   ← 主文件（由 tools/sync_bank.js 生成），可拆分章节为多个
1.5_填空题.json
02章_填空题.json
```

## 更新日志

| 日期 | 改动 |
|---|---|
| 2026-09-11 | v1 初版：1.1（10题）+ 1.2（12题），共 22 题；填空题型 + 即时判分 + 错题本 + 进度联动读侧 |
| 2026-09-13 | v2：新增 1.3「现代化创新发展」12 题（共 34 题）；**答对自动同步勾选**知识点清单（写入 `checklist:` 命名空间）；修复 v1 勾选检测正则未含 `checklist:` 前缀导致面板永远「未检测到」的 Bug；题库.json 与 HTML 内嵌题库逐字段同步校验通过 |
| 2026-09-14 | v3：新增 1.4「数字中国」12 题（共 46 题）。**修 2 个 Bug**：① `renderTabs()` 里 chapters 被硬编码为 `["1.1","1.2"]`，导致 1.3（及今后章节）的 tab 根本不渲染 —— 改为 `Object.keys(QUESTION_BANK)` 动态生成（v2 日志里「章节 tab 动态生成」的描述与实际不符，已更正）；② `jumpToQuestion()` 里 `ch === "1.1" \|\| ch === "1.2" ? ch : …` 使得 1.3+ 的错题点进去切不过章节 —— 改为直接 `state.currentChapter = ch`。新增 `tools/sync_bank.js` 反向生成题库.json，杜绝手工双写不同步。 |
| 2026-09-14 | v4：新增 1.5「数字化转型与元宇宙」12 题（共 58 题）。1.5 是**第 4 版新增小节**，题目按「成组名词辨识」设计（四科学范式 / 智慧转移两过程八步 / 元宇宙四特征各设对照题），hint 中直接标注业界口径陷阱（「低延迟 / 随地 / 文明」非教材口径）。check 字段用 1.5 清单的**语义键 A0~E3**，答对后自动勾选对应条目。 |